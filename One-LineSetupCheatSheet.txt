# Clone repo
git clone https://github.com/MarvinOS-online/fitlab-core.git
cd fitlab-core

# Build & start stack (one-shot containers run automatically)
docker compose up --build -d

# Access UI
echo "LLM & Image UI: http://localhost:8080"
echo "HTTPS Ingress: https://localhost"
echo "Stable Diffusion API: http://localhost:7860"

# Restart / Stop stack
# docker compose restart
# docker compose down

# Pull additional models (example)
# docker compose up pull-llm-ollama-yourmodel

# View logs (follow real-time)
# docker compose logs -f open-webui
# docker compose logs -f ollama
